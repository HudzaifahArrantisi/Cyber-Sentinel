# Network Security Analyzer - Dependencies

# Core Dependencies
colorama>=0.4.6
scapy>=2.5.0
psutil>=5.9.0
requests>=2.31.0
netifaces>=0.11.0
pyyaml>=6.0
tabulate>=0.9.0
progress>=1.6
click>=8.1.0

# Network Scanning & Analysis
python-nmap>=0.7.1
# Note: Requires nmap binary installed on system
# For Windows: https://nmap.org/download.html

# Windows-specific Dependencies (optional)
pywin32>=306; platform_system == "Windows"
wmi>=1.5.1; platform_system == "Windows"

# Database Support (optional)
sqlalchemy>=2.0.0
# Choose one database driver:
# SQLite (included with Python)
# PostgreSQL: psycopg2-binary>=2.9.0
# MySQL: mysql-connector-python>=8.0.0

# Web Interface (optional)
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0

# Data Visualization (optional)
matplotlib>=3.7.0
networkx>=3.0

# Cryptography & Security
cryptography>=41.0.0

# Development Dependencies (not required for production)
pytest>=7.4.0
pytest-cov>=4.1.0
flake8>=6.0.0
black>=23.0.0
mypy>=1.0.0
pylint>=3.0.0
bandit>=1.7.0
safety>=2.0.0

# Documentation
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0

# Testing & Mocking
coverage>=7.0.0
pytest-mock>=3.10.0

# Performance Profiling
memory-profiler>=0.60.0

# Version Compatibility Note:
# This project requires Python 3.8 or higher

# Installation note for Linux:
# You may need to install these system packages:
# - libpcap-dev (for packet capture)
# - tcpdump
# - nmap
# 
# Ubuntu/Debian: sudo apt-get install libpcap-dev tcpdump nmap
# Fedora/RHEL: sudo dnf install libpcap-devel tcpdump nmap
# Arch: sudo pacman -S libpcap tcpdump nmap

# Installation note for Windows:
# - Install Nmap from: https://nmap.org/download.html
# - Run installer as Administrator
# - Add Nmap to PATH
# - May need to install WinPcap/Npcap for packet capture

# For packet capture on macOS:
# brew install libpcap